# Админ-панель ehosting.lv - Laravel 12

Полнофункциональная административная панель в стиле Laravel Nova для управления приложением.

## 🚀 Возможности

### Основные модули
- **Дашборд** - главная страница с метриками и статистикой
- **Управление пользователями** - полный CRUD для пользователей
- **Поиск и фильтрация** - быстрый поиск по всем разделам
- **Аутентификация** - защита админ-панели middleware

### Интерфейс
- Современный дизайн в стиле Laravel Nova
- Адаптивная верстка для всех устройств
- Темная боковая панель с навигацией
- Интерактивные графики (Chart.js)
- Анимации и переходы (Alpine.js)
- Tailwind CSS для стилизации

### Функционал
- **Автокомплит** - умный поиск с подсказками
- **Сортировка** - по любым колонкам в таблицах
- **Пагинация** - удобная навигация по записям
- **Валидация** - клиентская и серверная
- **Уведомления** - красивые toast-сообщения
- **Горячие клавиши** - быстрый доступ (Ctrl+K для поиска)

## 📦 Установка

### 1. Запустите миграции

```bash
php artisan migrate
```

Это создаст поле `is_admin` в таблице `users`.

### 2. Создайте администратора

```bash
php artisan db:seed --class=AdminUserSeeder
```

Будет создан администратор с данными:
- **Email:** admin@ehosting.lv
- **Пароль:** password

⚠️ **ВАЖНО:** Измените пароль после первого входа!

### 3. Настройте аутентификацию

Для использования админ-панели необходима базовая аутентификация Laravel. 

Если у вас еще нет системы входа, установите Laravel Breeze или Jetstream:

```bash
# Laravel Breeze (рекомендуется)
composer require laravel/breeze --dev
php artisan breeze:install
npm install && npm run dev
```

### 4. Откройте админ-панель

Запустите сервер разработки:

```bash
php artisan serve
```

Перейдите по адресу: **http://localhost:8000/admin**

## 🎨 Структура файлов

```
app/
├── Http/
│   ├── Controllers/Admin/
│   │   ├── DashboardController.php     # Главная страница
│   │   └── UserController.php          # Управление пользователями
│   └── Middleware/
│       └── AdminMiddleware.php         # Проверка прав доступа
├── Models/
│   └── User.php                        # Расширенная модель пользователя

resources/
└── views/admin/
    ├── layout.blade.php                # Основной шаблон
    ├── dashboard.blade.php             # Дашборд
    └── users/
        ├── index.blade.php             # Список пользователей
        ├── create.blade.php            # Создание
        ├── edit.blade.php              # Редактирование
        └── show.blade.php              # Просмотр

public/admin/
├── admin.css                           # Кастомные стили
└── admin.js                            # JavaScript функционал

database/
├── migrations/
│   └── *_add_is_admin_to_users_table.php
└── seeders/
    └── AdminUserSeeder.php             # Создание администратора

routes/
└── web.php                             # Роуты админ-панели
```

## 🔐 Безопасность

### Middleware защита

Все административные маршруты защищены `AdminMiddleware`:

```php
// Раскомментируйте в app/Http/Middleware/AdminMiddleware.php
if (!auth()->user()->is_admin) {
    abort(403, 'У вас нет доступа к административной панели');
}
```

### Права доступа

Проверка прав администратора в модели `User`:

```php
// Проверка в коде
if (auth()->user()->isAdmin()) {
    // Действие для администратора
}

// Scope для выборки
$admins = User::admins()->get();
$users = User::regularUsers()->get();
```

## 🎯 Использование

### Управление пользователями

**Список:** `GET /admin/users`
- Поиск по имени и email
- Сортировка по колонкам
- Пагинация (15 записей)

**Создание:** `GET /admin/users/create`
- Форма с валидацией
- Автокомплит полей

**Просмотр:** `GET /admin/users/{id}`
- Детальная информация
- Статистика активности

**Редактирование:** `GET /admin/users/{id}/edit`
- Обновление данных
- Изменение пароля (опционально)

**Удаление:** `DELETE /admin/users/{id}`
- С подтверждением

### Дашборд

**Главная:** `GET /admin`
- Карточки с метриками
- График регистраций (7 дней)
- Быстрые действия

## 🛠 Кастомизация

### Добавление новых разделов

1. Создайте контроллер:
```bash
php artisan make:controller Admin/PostController --resource
```

2. Добавьте роуты в `routes/web.php`:
```php
Route::resource('posts', PostController::class);
```

3. Создайте views в `resources/views/admin/posts/`

4. Добавьте пункт в меню (`resources/views/admin/layout.blade.php`)

### Изменение стилей

Отредактируйте `public/admin/admin.css`:
- CSS переменные в `:root`
- Кастомные классы `.nova-*`

### Добавление функционала

В `public/admin/admin.js` доступны функции:
```javascript
// Показать уведомление
window.adminPanel.showNotification('Сообщение', 'success');

// Копировать в буфер
window.adminPanel.copyToClipboard('Текст');

// Поиск
window.adminPanel.performSearch('запрос');
```

## 📊 Зависимости

### Backend
- Laravel 12
- PHP 8.2+

### Frontend
- Tailwind CSS (CDN)
- Alpine.js (CDN)
- Chart.js (CDN)

### Рекомендуется
- Laravel Breeze/Jetstream для аутентификации

## 🔧 Настройка для production

1. **Смените учетные данные администратора**
2. **Настройте HTTPS**
3. **Используйте локальные версии CSS/JS** вместо CDN:

```bash
npm install tailwindcss alpinejs chart.js
```

4. **Настройте кеширование**:
```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

5. **Проверьте middleware** - раскомментируйте проверку `is_admin`

## 📝 Лицензия

Разработано **ehosting.lv** © 2025

---

## 🆘 Поддержка

Для вопросов и поддержки обращайтесь:
- Email: support@ehosting.lv
- Web: https://ehosting.lv

## ✨ Особенности реализации

- ✅ Полная документация кода (PHPDoc)
- ✅ Типизация и автокомплит
- ✅ Готовые миграции и seeders
- ✅ Валидация форм
- ✅ Обработка ошибок
- ✅ Адаптивный дизайн
- ✅ Доступность (a11y)
- ✅ SEO-friendly
- ✅ Поддержка горячих клавиш
- ✅ Автосохранение форм (draft mode)

## 🚀 Roadmap

Планируемые улучшения:
- [ ] Расширенные роли и права
- [ ] Многоязычность (i18n)
- [ ] Темная тема
- [ ] Экспорт данных (Excel, CSV)
- [ ] API для мобильных приложений
- [ ] Двухфакторная аутентификация
- [ ] Логирование действий администратора
- [ ] Уведомления в реальном времени (WebSocket)

---

**Создано с ❤️ командой ehosting.lv**
